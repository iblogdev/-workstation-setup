- hosts: localhost
  connection: local
  tags: always
  become: true
  tasks:
  - homebrew:
      name: curl
      state: present
    environment:
      PATH: /home/linuxbrew/.linuxbrew/bin:{{ ansible_facts.env.PATH }}
